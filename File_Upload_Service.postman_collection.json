{
  "info": {
    "name": "File Upload & Processing Service",
    "_postman_id": "file-upload-service-v1",
    "description": "Complete API collection for testing the File Upload & Processing Service",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "1. Health Check",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/healthz",
          "host": ["{{base_url}}"],
          "path": ["healthz"]
        },
        "description": "Check if the service and its dependencies (MongoDB, S3) are healthy"
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status is 200\", function() {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "pm.test(\"Service status exists\", function() {",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.status).to.exist;",
              "    console.log(\"Service status:\", jsonData.status);",
              "    console.log(\"MongoDB:\", jsonData.services.mongodb);",
              "    console.log(\"S3:\", jsonData.services.s3);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "2. Upload File",
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "formdata",
          "formdata": [
            {
              "key": "file",
              "type": "file",
              "src": [],
              "description": "Select a file to upload (JSON, CSV, or text file)"
            }
          ]
        },
        "url": {
          "raw": "{{base_url}}/upload",
          "host": ["{{base_url}}"],
          "path": ["upload"]
        },
        "description": "Upload a file to S3. The file will be streamed directly without buffering in memory."
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Upload successful\", function() {",
              "    pm.response.to.have.status(200);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.fileId).to.exist;",
              "    pm.expect(jsonData.s3Key).to.exist;",
              "    ",
              "    // Save fileId to environment for next request",
              "    pm.environment.set(\"fileId\", jsonData.fileId);",
              "    ",
              "    console.log(\"✓ File uploaded successfully\");",
              "    console.log(\"File ID:\", jsonData.fileId);",
              "    console.log(\"S3 Key:\", jsonData.s3Key);",
              "    console.log(\"Size:\", jsonData.metadata.size, \"bytes\");",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "3. Create Processing Job",
      "request": {
        "method": "POST",
        "header": [],
        "url": {
          "raw": "{{base_url}}/process/{{fileId}}",
          "host": ["{{base_url}}"],
          "path": ["process", "{{fileId}}"]
        },
        "description": "Create a job to process the uploaded file. The file will be downloaded from S3, parsed line-by-line, and inserted into MongoDB."
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Job created successfully\", function() {",
              "    pm.response.to.have.status(201);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.jobId).to.exist;",
              "    pm.expect(jsonData.state).to.eql(\"queued\");",
              "    ",
              "    // Save jobId for next request",
              "    pm.environment.set(\"jobId\", jsonData.jobId);",
              "    ",
              "    console.log(\"✓ Job created and queued\");",
              "    console.log(\"Job ID:\", jsonData.jobId);",
              "    console.log(\"State:\", jsonData.state);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "4. Check Job Status",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/jobs/{{jobId}}",
          "host": ["{{base_url}}"],
          "path": ["jobs", "{{jobId}}"]
        },
        "description": "Check the status of a processing job. States: queued → in_progress → completed/failed"
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Job status retrieved\", function() {",
              "    pm.response.to.have.status(200);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.jobId).to.exist;",
              "    pm.expect(jsonData.state).to.be.oneOf([\"queued\", \"in_progress\", \"completed\", \"failed\"]);",
              "    ",
              "    console.log(\"Job State:\", jsonData.state);",
              "    console.log(\"Progress:\");",
              "    console.log(\"  - Lines Processed:\", jsonData.progress.linesProcessed);",
              "    console.log(\"  - Records Inserted:\", jsonData.progress.recordsInserted);",
              "    console.log(\"  - Errors:\", jsonData.progress.errors);",
              "    ",
              "    if (jsonData.state === \"completed\") {",
              "        console.log(\"✓ Job completed successfully!\");",
              "    } else if (jsonData.state === \"failed\") {",
              "        console.log(\"✗ Job failed:\", jsonData.result);",
              "    }",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "5. Service Info",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/",
          "host": ["{{base_url}}"],
          "path": [""]
        },
        "description": "Get service information and available endpoints"
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Service info retrieved\", function() {",
              "    pm.response.to.have.status(200);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.service).to.exist;",
              "    pm.expect(jsonData.version).to.exist;",
              "    console.log(\"Service:\", jsonData.service);",
              "    console.log(\"Version:\", jsonData.version);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Error: Process Non-existent File",
      "request": {
        "method": "POST",
        "header": [],
        "url": {
          "raw": "{{base_url}}/process/507f1f77bcf86cd799439011",
          "host": ["{{base_url}}"],
          "path": ["process", "507f1f77bcf86cd799439011"]
        },
        "description": "Test error handling: try to process a file that doesn't exist"
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Returns 404 for non-existent file\", function() {",
              "    pm.response.to.have.status(404);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.error).to.exist;",
              "    console.log(\"Error message:\", jsonData.error);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Error: Invalid File ID Format",
      "request": {
        "method": "POST",
        "header": [],
        "url": {
          "raw": "{{base_url}}/process/invalid-id",
          "host": ["{{base_url}}"],
          "path": ["process", "invalid-id"]
        },
        "description": "Test error handling: try to process with invalid file ID format"
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Returns 400 for invalid file ID\", function() {",
              "    pm.response.to.have.status(400);",
              "    const jsonData = pm.response.json();",
              "    pm.expect(jsonData.error).to.exist;",
              "    console.log(\"Error message:\", jsonData.error);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request script",
          "console.log(\"Running request:\", pm.info.requestName);",
          "console.log(\"Base URL:\", pm.environment.get(\"base_url\") || \"Not set\");"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level test script",
          "console.log(\"Response time:\", pm.response.responseTime, \"ms\");"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "fileId",
      "value": "",
      "type": "string"
    },
    {
      "key": "jobId",
      "value": "",
      "type": "string"
    }
  ]
}

